# Gemini API 金鑰（注意：沒有 VITE_ 前綴）
#
# 沒有 VITE_ 前綴，Vite 就不會把它打包進前端 JS，
# 金鑰只會被伺服器端的 api/scan.ts 讀取，瀏覽器永遠看不到。
#
# 線上環境請改到 Vercel 後台設定：
#   Settings → Environment Variables → 新增 GEMINI_API_KEY
GEMINI_API_KEY=your_api_key_here
